import Vue from 'vue'
import Vuex, { Store, StoreOptions } from 'vuex'

Vue.use(Vuex)

export const store: any = new Vuex.Store({
  state: {
  },
  actions: {
  },
  mutations: {
  },
  getters: {
  }
})
